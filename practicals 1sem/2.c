#include <stdio.h>
void main()
{
    int a,b,sum;
    printf("Enter the first Number:");
    scanf("%d",&a);
    printf("Enter the Second Number:");
    scanf("%d",&b);
    sum=a+b;
    printf("Sum of the given two Numbers =%d",sum);

}